import React from "react";
import { View, Text, Image, StyleSheet, TouchableOpacity } from "react-native";

const Profile = () => {
  return (
    <View>
      <Text>je suis de profile page</Text>
    </View>
  );
};
export default Profile;

const styles = StyleSheet.create({});
